import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Appli_suivie_serie_film',
  webDir: 'www'
};

export default config;
